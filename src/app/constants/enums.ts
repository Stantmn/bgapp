export enum PaginationButton {
    Next = 'next',
    Prev = 'prev'
}

export enum Roles {
    Admin = 'admin',
    User = 'user',
    StoreAdmin = 'store admin',
    UserProduct = 'user product',
    UserOrder = 'user order'
}
